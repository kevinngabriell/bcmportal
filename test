console.log("🔍 resolveValueFromItem:", {
  label: item.label,
  suffix,
  keyFromMap: item.keyMapBySuffix?.[suffix],
  fallbackKeys: item.keys,
  finalValue: item.keyMapBySuffix?.[suffix]
    ? row[item.keyMapBySuffix[suffix]]
    : getValueByKeys(row, item.keys ?? [], suffix)
});