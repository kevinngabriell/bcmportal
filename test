function aoaToJson(data: (string | null)[][]): { field: string, value: string | null, title: string }[] {
  return data
    .filter(row => row.length >= 2 && row[0]) // skip row kosong
    .map(row => ({
      field: String(row[0]),
      value: row[1] ?? null,
      
    }));
}
